# 📅 Sistema de Agendamento FAQUI - TI

Sistema desenvolvido em Python (Streamlit) para reserva de equipamentos e salas.

## 🚀 Como rodar em um Servidor VPS (Linux/Ubuntu)

1. **Atualizar o servidor e instalar o Python:**
   ```bash
   sudo apt update
   sudo apt install python3 python3-pip python3-venv